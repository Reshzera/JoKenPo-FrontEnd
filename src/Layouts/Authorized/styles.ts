import styled from 'styled-components';

export const AuthorizedLayoutContainer = styled.div`
  display: flex;
  max-width: 100vw;
  min-width: 100vw;
  min-height: 100vh;
  position: relative;
`;

export const AuthorizedLayoutContent = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
  overflow-x: hidden;
  overflow-y: auto;
`;
